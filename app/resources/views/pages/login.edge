@component('components/layout/app', { title: 'Login | NordicSkywatch' })
    @slot('meta')
        <meta name="description" content="Admin login site" />
    @end

    @slot('main')
        <div class="bg-zinc-900 min-h-full py-24 sm:py-32">
            <div class="w-full max-w-4xl mx-auto p-4 sm:py-8">
                <h1 class="text-base/7 font-semibold text-gray-300">Login Page</h1>
            </div>
        </div>
    @end
@end